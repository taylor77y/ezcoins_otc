<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezcoins.project.acl.mapper.RoleMenuMapper">

    <insert id="insertList">
        INSERT INTO acl_role_menu VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId},#{item.menuId})
        </foreach>
    </insert>

    <delete id="deleteBatchMenus">
        DELETE FROM acl_role_menu
        WHERE role_id = #{roleId}
        AND menu_id IN
        <foreach collection="menuIds" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
</mapper>
