<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezcoins.project.consumer.mapper.EzUserMapper">
  <resultMap id="BaseResultMap" type="com.ezcoins.project.consumer.entity.EzUser">
    <!--@mbg.generated-->
    <!--@Table ez_user-->
    <id column="user_id"  property="userId" />
    <result column="parent_id"  property="parentId" />
    <result column="nick_name"  property="nickName" />
    <result column="user_name" property="userName" />
    <result column="password"  property="password" />
    <result column="phone"  property="phone" />
    <result column="email"  property="email" />
    <result column="invite_code"  property="inviteCode" />
    <result column="security_password"  property="securityPassword" />
    <result column="level"  property="level" />
    <result column="kyc_status"  property="kycStatus" />
    <result column="status"  property="status" />
    <result column="login_ip"  property="loginIp" />
    <result column="login_date"  property="loginDate" />
    <result column="create_by"  property="createBy" />
    <result column="create_time"  property="createTime" />
    <result column="update_by"  property="updateBy" />
    <result column="update_time"  property="updateTime" />
    <result column="remark"  property="remark" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    user_id, parent_id, nick_name, user_name, `password`, phone, email, invite_code,
    security_password, `type`, kyc_status, `status`, login_ip, login_date, create_by, 
    create_time, update_by, update_time, remark
  </sql>

  <select id="checkUserUnique" resultType="java.lang.Integer">
    select count(1) from ez_user
    <where>
      <if test="userName != null and userName != ''">
        and user_name = #{userName}
      </if>
      <if test="phone != null and phone != ''">
        and phone = #{phone}
      </if>
      <if test="email != null and email != ''">
        and email = #{email}
      </if>
      <if test="inviteCode != null and inviteCode != ''">
        and invite_code = #{userName}
      </if>
    </where>
  </select>

  <select id="selectUserBy" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from ez_user
    <where>
      <if test="userName != null and userName != ''">
        and user_name = #{userName}
      </if>
      <if test="phone != null and phone != ''">
        and phone = #{phone}
      </if>
      <if test="email != null and email != ''">
        and email = #{email}
      </if>
      <if test="inviteCode != null and inviteCode != ''">
        and invite_code = #{userName}
      </if>
    </where>
  </select>
</mapper>